package resolver;

import cube.BigCube;

import java.util.PriorityQueue;

/**
 * TODO в процессе.
 */
public class Helper {
    public PriorityQueue<BigCube> expandTheState(BigCube previousState) {
        PriorityQueue<BigCube> expandedStates = new PriorityQueue<>();

        return expandedStates;
    }
}
